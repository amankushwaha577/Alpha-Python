# Que. Default vs keyword arguments?
# → Default = predefined (def f(x=5)), keyword = pass by name (f(x=10)).
#
# Que. What are *args and **kwargs?
# → *args = variable-length positional args, **kwargs = variable-length keyword args.
#
# Que. What are lambda functions?
# Anonymous one-line functions → lambda x: x*x.