# Q. How to reverse a string?
# → s[::-1] or ''.join(reversed(s)).
#
# Q. Palindrome check?
# → Compare with reverse (s == s[::-1]).
#
# Q. Difference between is and ==?
# → is checks identity (memory address), == checks value.

